export const createShowObject = (showId, showName, showImg, isFavorite) => {
    return {
        showId: showId,
        showName: showName,
        showImg: showImg,
        isFavorite: isFavorite
    }
}
